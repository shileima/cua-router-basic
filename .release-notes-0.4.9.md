## 0.4.9

本版本核心是引入 AX Tree 缓存与自动失效机制，在保持定位正确性的前提下大幅减少 `sky.get_app_state` 调用；同时提供轮询异步 UI 场景的兜底方案，并补齐维护者文档。

### 🚀 新特性

- **AX 缓存 helpers**：新增 `globalThis.ax`（`get / invalidate / findIdx / findAllIdx / findFocusedIdx / linesMatching / summarize`），bootstrap 后自动可用，无需在每段代码块里重复定义。
- **`ax.get(app)` 内部固定 `disableDiff:true`**：跨 `/exec` 缓存，正确性等价于每次都取新树。
- **sky 交互自动失效缓存**：`click / double_click / right_click / drag / scroll / set_value / type_text / press_key / key_down / key_up / hover / mouse_move` 调用后（含失败），自动 invalidate 对应 app 缓存。
- **`ax.get(app, { refresh })`**：强制重取。
- **`ax.get(app, { maxAgeMs })`**：缓存年龄 ≥ N ms 判定陈旧，兜住"外部触发未 refresh"风险（swift / AppleScript / 页面自身异步更新）。`maxAgeMs:0` 语义等价 `refresh:true`。
- **调试接口**：`ax._stats()` 返回 `hits / misses / staleMisses / refreshes / invalidations / hitRate / cacheSize / entries[]`；`ax._resetStats()` 归零。
- **输出收敛**：`ax.summarize(state, opts)` 只回传必要字段，避免把整棵树塞进 `nodeRepl.write`。

### 📈 性能

真实场景实测（RPA 工作流调试页面，6 次 × 1.2s 轮询）：

| 写法 | 真调用次数 | 结果 |
|---|---|---|
| `ax.get(app)` 主线（有 sky 交互） | 大幅下降 | 命中缓存 |
| `ax.get(app, { refresh: true })` | 6 | 每次最新 |
| `ax.get(app, { maxAgeMs: 300 })` | 6 | 每次最新，写法更清晰 |

### 📚 文档

- 用户文档 `SKILL.md` + `references/*.md` 全部改用 `ax.*` 示例，一次取树多次搜索。
- `references/ax-locating.md` 新增"轮询等待异步 UI 更新"章节：附实测证据表与标准模板。
- 新增维护者文档 `docs/`：
  - `docs/README.md` 索引 + 快速心智模型
  - `docs/architecture.md` 分层图与关键文件
  - `docs/ax-cache-design.md` 缓存语义、决策路径与修改 checklist
  - `docs/risks.md` 6 类风险清单与复现方法
  - `docs/sky-and-vendor-sync.md` sky API 变更与 vendor 同步指南
  - `docs/release-workflow.md` 版本号约定、本地 beta 与正式发布路径
  - `docs/troubleshooting.md` 症状定位与修复动作

### 🧪 测试

- Node 单元测试从 0 → 23 用例，覆盖 helpers / 缓存命中 / mutation 自动失效 / maxAgeMs TTL / refresh 优先级 / stats 计数 / 坐标点击全清 / 失败也失效。
- Python 单元测试保持 2 用例。

### 🔧 兼容性

- 老写法 `sky.get_app_state({ app, disableDiff: true })` 依然工作，wrapper 会回填缓存与 `ax.get` 共用同一份快照，可与新写法自由混用。
- `disableDiff: true` 完整快照语义不变。
- 白名单外的 sky 方法透传，不影响。

### ⚠️ 升级注意

- **必须 `daemon.sh restart`**：`computer-use-client.mjs` 变化后，已 bootstrap 的 node_repl 不会自动重跑 `setupComputerUseRuntime`，重启才能生效。
- **轮询异步 UI 场景**：切勿用普通 `ax.get(app)` 轮询，会陷入缓存陈旧；推荐 `ax.get(app, { maxAgeMs: 300 })` 或 `{ refresh: true }`。见 `docs/risks.md` R1.1。
- **多 agent 并发**：当前设计假设单 agent，多 agent 场景需谨慎（`globalThis` 共享 AX 缓存）。见 `docs/risks.md` R3.1。
